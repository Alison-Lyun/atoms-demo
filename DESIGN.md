# 设计决策

需求见 [SPEC.md](SPEC.md)，具体状态与模块约束见 [架构说明](docs/architecture.md)，已执行的检查见 [验收记录](docs/acceptance.md)。

## 最小闭环

用户描述需求 → 服务端调用真实模型 → 校验完整 HTML 产物 → 浏览器启动候选预览 → 原子发布代码与应用数据 → 同项目继续修改或恢复旧代码。

采用自包含 HTML、CSS、JavaScript，将构建依赖和外部网络移出生成环节。工作台本身使用 Next.js、React 和 TypeScript；这一选择降低短期交付的不确定性，但不支持生成多文件 React 工程或后端服务。

## 四个核心约束

1. **候选与当前版本分开。** 失败候选不能覆盖当前成功版本；浏览器启动通过后仍要核对版本与数据 revision。
2. **先存储确认，再通知成功。** 已发布应用通过 `window.appStorage` 向宿主请求持久化；候选写入先暂存，晋升时与代码一并提交。
3. **身份由服务端确认。** Supabase 匿名 Auth 为浏览器建立身份；RLS 限制读取，事务 RPC 校验 `auth.uid()`、所有权和预期 revision。应用不使用 service-role key。
4. **错误有界且可恢复。** 静态检查和启动异常最多触发两次修复，任务总截止时间 180 秒；取消后的迟到结果不能发布，持续 revision 冲突返回 409。

## 数据与接口

`projects` 保存项目与当前版本；`messages` 保存对话；`versions` 保存完整 HTML 及候选/成功/失败状态；`runs` 保存生成、恢复、取消和截止时间；`app_state` 保存业务 JSON。完整快照在事务中通过比较 revision 提交。

生成和验证接口返回 NDJSON 状态事件，工作台同时能通过项目读取接口恢复持久化状态。代码恢复新增候选版本并使用当前业务数据；没有历史数据快照，也不承诺恢复过去的数据。

## 隔离与边界

iframe 仅开放 `allow-scripts`，不开放同源权限。CSP 限制网络和导航；存储消息校验来源窗口、随机 nonce 及 JSON 大小、键名和深度。静态检查不能证明任意程序安全，iframe 也没有进程级 CPU 或内存配额。

当前适用于笔试演示与有限访问。正式服务仍需要账户找回、账户/IP 级限流、访问控制、匿名注册防滥用，以及更强的执行资源隔离。具体已验证与未验证范围以验收记录为准。
